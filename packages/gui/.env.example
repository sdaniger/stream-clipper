# Base URL for the shared Twitch chat API (Next.js route on port 3000)
# The Vite browser app calls this to fetch Twitch chat data.
# Override if your Next.js dev server runs on a different host/port.
VITE_CHAT_API_BASE_URL=http://localhost:3000
